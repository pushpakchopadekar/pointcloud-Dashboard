# Optional: override the point cloud source.
# Leave unset to load directly from the 3D Bharat URL.
# If that host blocks cross-origin requests from your dev server (CORS),
# download the .ply into /public and point this at the local path instead:
# VITE_PLY_PATH=/Road_Design_Charholi.ply
VITE_PLY_PATH=
